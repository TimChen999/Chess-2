# Claude local config (per-machine)
.claude/

# Local secrets (API keys for sprite-gen tooling, etc.)
.env
.env.local
.env.*.local

# Python bytecode cache (sprite-gen tooling)
__pycache__/
*.pyc

# PixelLab inpaint cache (regeneratable, large)
tools/sprites/_pixellab_cache/

# Godot 4 editor cache & import data
godot/.godot/
godot/.import/
godot/export.cfg
godot/export_presets.cfg

# Godot Mono / C# (unused here, but standard)
godot/.mono/
godot/data_*/
godot/mono_crash.*.json
